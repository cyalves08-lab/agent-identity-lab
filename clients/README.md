# Clients

Completed **BrandEdge™ Authority Reports** are saved here, one file per client:
`clients/<client-name>-brandedge.md`.

The `brandedge-strategist` agent writes these after running the Agent Identity
Lab™ interview. Keep them so the team can reference a client's archetype,
positioning, and voice when drafting content or replies for them.

Treat these as confidential client work product.
